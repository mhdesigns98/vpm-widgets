# PBS Show Playlist — Handoff
*Status: working — verified end-to-end in a real browser, not yet shipped*
*Last updated: 2026-08-31 · Resolved the PBS 401, fixed a sort bug and a clip-vs-episode filtering bug, and fixed a broken player-embed integration · Last verified: 2026-08-31 — loaded the widget in a real browser via a local static server, confirmed 5 real full-length episodes render with working video playback and click-to-swap*
*Live vs repo: repo only — nothing pasted into WordPress yet; only the GitHub Pages preview will reflect this once pushed*

## Current state
Widget (`index.html`) and its Cloudflare Worker (`~/Projects/vpm/pbs-api/`) both work end-to-end against the real PBS Media Manager API. The 401 that blocked this project since 2026-08-29 is resolved — PBS confirmed the credentials were valid, and re-entering them fresh into the Worker's secrets fixed it (root cause never fully pinned down: likely a stale/corrupted secret from the original `wrangler secret put`, since PBS insisted the credentials were already active). Verified in a real browser: the main player loads the latest episode automatically, the 5-thumbnail strip shows five distinct real episodes with correct titles/dates/images, and clicking a thumbnail correctly swaps the player and updates the "Now Playing" label and selection indicator.

## Decisions made (and why)
- **Re-entered CF Worker secrets fresh, didn't chase further root-cause** — PBS's own docs-example endpoint (`GET /shows/{id}/`) returned `{"detail":"Invalid key/secret."}"` even after PBS confirmed the credentials were active/production. Re-running `wrangler secret put` for both `PBS_API_KEY` and `PBS_API_SECRET` with freshly copy-pasted values fixed it immediately. Not worth further investigation now that it works — see Gotchas if this recurs.
- **Switched the `sort` param from `-air_date` to `-encored_on`** — `air_date` isn't a real PBS field (confirmed via the `meta.sort` block in PBS's own response, which only lists `encored_on`/`premiered_on`/`updated_at`); the previous code was silently sorting by an ignored/default order instead of true recency. The README always documented `-encored_on` as intended — this was a code/doc mismatch, now fixed.
- **Added `type=full_length` to the `/assets/` filter** — without it, PBS returns a mix of `full_length`/`clip`/`preview` assets, and Virginia Home Grown posts many short topical clips per episode. The un-filtered query was returning 5 clips from a single episode, not 5 distinct episodes — a direct contradiction of the brief's "5 episode thumbnails." Confirmed `type` is a valid filter via the `meta.filter` block in PBS's own response.
- **Rewrote the player-embed logic to use PBS's own `player_code` field instead of a constructed URL** — the original code tried to build `https://player.pbs.org/widget/partnerplayer/{tp_media_object_id}/?callsign=...`, but real PBS asset responses have no `tp_media_object_id` field at all, and the real player URL shape is completely different (`https://player.pbs.org/partnerplayer/{token}/?topbar=...&autoplay=...`, no callsign param). This was designed against assumed API shape before real credentials ever worked, and was never actually exercised until today. Now the widget extracts the `src` straight out of the `player_code` iframe HTML PBS already returns, and toggles `autoplay=true/false` on that same URL for the autoplay-to-next behavior. `CALLSIGN` config is now dead and was removed.
- **Removed the temporary `/_health` diagnostic route** added mid-session to isolate whether the 401 was PBS-side or ours (it hit PBS's plain `/shows/{id}/` doc example, bypassing the `/assets/` filter). Not needed now that the real issue (bad secrets) is fixed and confirmed.

## In progress / next steps
- [ ] Commit these fixes in both repos (`pbs-api` and `vpm-widgets`) — currently uncommitted working-tree changes.
- [ ] Push the `pbs-api` Worker repo to GitHub (still local-only, no remote configured, per prior session).
- [ ] Resolve the PBS API Terms of Service open question: confirm with PBS/station relations whether any sponsor/underwriting content on the VHG show page conflicts with the ToS ban on selling "advertising, sponsorships, or promotions... in association with the PBS Content or PBS Player." Still unresolved from the prior session — this could get API access revoked if unresolved.
- [ ] Add a visible link to PBS's Terms of Use somewhere in the widget — the PBS API ToS requires displaying this to end users; not yet added.
- [ ] Manually verify the autoplay-to-next behavior fires on real video completion (only click-to-swap was tested this session; the `postMessage`-driven autoplay path needs a full episode to actually finish, or the PBS player's `complete` event triggered manually).
- [ ] Run `/ship-widget pbs-show-playlist` before pasting the final iframe into WordPress — required pre-CMS gate, not yet run.
- [ ] Consider whether ad-blocking network calls seen in dev console (doubleclick.net, litix.io) will behave differently/cause console noise on the live vpm.org page — worth a spot-check post-deploy, though likely harmless (ad server calls failing gracefully is normal PBS partner-player behavior, not a widget bug).

## Gotchas / things that will bite you
- **The 401 was resolved by simply re-entering the same credentials PBS said were already valid** — root cause was never conclusively identified. If this widget (or any future PBS Media Manager integration) hits a 401 again, don't assume it's automatically PBS-side just because support says the creds are active; try a clean `wrangler secret put` re-entry first, since that's what actually fixed it here.
- **PBS Media Manager has no flat episodes-by-show endpoint.** Season-aware episode listings need `shows/{id}/seasons` → `seasons/{id}/episodes`. This widget deliberately uses `/assets/?show-id=&type=full_length` instead, which is simpler and gives the player-ready `player_code` field directly.
- **`/assets/` real filter params, confirmed from PBS's own response `meta.filter` block**: `id`, `slug`, `show-id`, `show-slug`, `special-id`, `special-slug`, `episode-id`, `episode-slug`, `type`, `platform-id`, `platform-slug`. Valid `sort` fields (from `meta.sort`): `encored_on`, `premiered_on`, `updated_at` only — no `air_date`.
- **PBS's real player embed is the `player_code` field on each asset, not a URL you construct yourself.** It's a full `<iframe>` HTML string; extract `src` from it rather than assuming any ID field maps to a player URL path.
- **PBS docs site (docs.pbs.org) needs a JS-rendering browser, not a plain fetch** — WebFetch/curl return an empty shell. Use a real browser (Playwright navigate + snapshot) to read it. The full endpoint/filter/sort reference lives on the linked "Resources" sub-page, not the main "Media Manager API" page.
- **PBS API ToS forbids advertising/sponsorship "in association with" PBS content/player** — this is the biggest open compliance risk, not a technical one. Still unresolved.
- **Two Cloudflare Worker secrets needed**: `PBS_API_KEY` and `PBS_API_SECRET` (set via `npx wrangler secret put <NAME> --profile vpm`, run from inside `~/Projects/vpm/pbs-api` — wrangler errors with "Required Worker name missing" if run from the wrong directory since it can't find `wrangler.jsonc`).

## Key files
- `~/Projects/vpm/vpm-widgets/widgets/pbs-show-playlist/index.html` — the widget itself (player, thumbnail strip, autoplay logic)
- `~/Projects/vpm/vpm-widgets/widgets/pbs-show-playlist/BRIEF.md` — original brief with user moment, done criteria, out-of-scope list
- `~/Projects/vpm/vpm-widgets/widgets/pbs-show-playlist/README.md` — widget usage notes, config values to update per show
- `~/Projects/vpm/pbs-api/src/index.js` — the CF Worker: proxies PBS `/assets/` endpoint (full-length only), Basic Auth, 1-hour cache, CORS
- `~/Projects/vpm/pbs-api/wrangler.jsonc` — Worker config, `account_id` pinned to VPM's CF account (`e017b19d2e3e1827adbd6f5907d81aac`)
- `~/Projects/vpm/pbs-api/README.md` — deploy commands, route spec

## Session log
- 2026-08-31: PBS support confirmed credentials were active/production; re-entering them into Worker secrets fixed the 401. Diagnosed and fixed two further bugs surfaced once real data flowed: wrong sort field, and no `type=full_length` filter (was returning clips, not episodes). Fixed a player-embed bug that predated ever having working credentials (used a nonexistent `tp_media_object_id` field and a wrong player URL shape) by switching to PBS's own `player_code` field. Verified end-to-end in a real browser: episode load, thumbnail strip, click-to-swap all work. Not yet committed, pushed, or run through `/ship-widget`.
- 2026-08-29: Scaffolded widget + Worker, deployed Worker, wired real show ID/callsign, discovered and fixed wrong auth header + wrong API endpoint (via PBS's own PHP client source), confirmed remaining 401 is PBS-side (credentials not yet active, or wrong environment) — handed back to Mark to follow up with PBS.
- 2026-08-29 (earlier): Wrote BRIEF.md via `/brief`, scaffolded widget via `/new-widget`, reviewed PBS API Terms of Service and flagged the advertising/sponsorship clause as a real risk.
